[build]
  command = "npm run build:pixelheads-ui"
  publish = "dist/pixelheads-ui"

[build.environment]
  NODE_VERSION = "20"

# Angular workspace SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
